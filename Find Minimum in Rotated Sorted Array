class Solution {
public:
    int findMin(vector<int>& nums) {
        int x=nums.size(),min=nums[0];
        for(int i=1;i<x;i++){
            if(nums[i]<=min){
                min=nums[i];
            }
        }
        return min;
    }
};
