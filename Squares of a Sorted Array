class Solution {
public:
    vector<int> sortedSquares(vector<int>& A) {
        int a=A.size();
        for(int i=0; i<a; i++){
            A[i]*=A[i];
        }
        sort(A.begin(),A.end());
        return A;
    }
};
