class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int s=nums.size(),count=0;
        for(int i=0;i<s;i++){
            if(nums[i]!=val) nums[count++]=nums[i];
        }
        return count;
    }
};
